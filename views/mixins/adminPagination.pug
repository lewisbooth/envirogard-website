mixin adminPagination
  //- Check the required variables exist
  if page && numberOfPages && numberOfPages > 1
    .admin__pagination
      - let key = 1
      while key <= numberOfPages
        span(
          data-key=key 
          onclick="changePage(event)"
          class=(key === page ? 'active' : '')
        )= key
        - key++